import { parse, parseForESLint } from "next/dist/compiled/babel/eslint-parser";

export default {
	parse,
	parseForESLint,
	meta: {
		name: "eslint-config-next-fork/parser",
		version: "15.3.0-canary.13",
	},
};
